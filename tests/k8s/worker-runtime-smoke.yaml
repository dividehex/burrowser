apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bw-runtime-pvc
  namespace: burrowser
  labels:
    app.kubernetes.io/name: burrowser-worker
    burrowser/test: runtime-smoke
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: burrowser-local-path
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: Secret
metadata:
  name: bw-runtime-worker-auth
  namespace: burrowser
  labels:
    burrowser/test: runtime-smoke
type: Opaque
stringData:
  WORKER_CONTROLLER_CREDENTIAL: disposable-runtime-secret
  BURROWSER_AUTHENTICATOR_KEY: AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA
---
apiVersion: v1
kind: Pod
metadata:
  name: bw-runtime-worker
  namespace: burrowser
  labels:
    app.kubernetes.io/name: burrowser-worker
    burrowser/test: runtime-smoke
spec:
  automountServiceAccountToken: false
  serviceAccountName: burrowser-worker
  securityContext:
    seccompProfile:
      type: Localhost
      localhostProfile: burrowser/chromium.json
  containers:
  - name: worker
    image: burrowser-worker:dev
    imagePullPolicy: Never
    envFrom:
    - secretRef:
        name: bw-runtime-worker-auth
    env:
    - name: DEBUG
      value: pw:browser*
    ports:
    - name: rpc
      containerPort: 8080
    securityContext:
      runAsNonRoot: true
      runAsUser: 10001
      allowPrivilegeEscalation: false
      readOnlyRootFilesystem: true
      capabilities:
        drop: [ALL]
    resources:
      requests: {cpu: 250m, memory: 512Mi}
      limits: {cpu: 1, memory: 1Gi, ephemeral-storage: 1Gi}
    readinessProbe:
      httpGet: {path: /health, port: rpc}
      initialDelaySeconds: 2
      periodSeconds: 2
    volumeMounts:
    - {name: profile, mountPath: /profile}
    - {name: tmp, mountPath: /tmp}
    - {name: shm, mountPath: /dev/shm}
    - {name: home, mountPath: /home/browser}
  volumes:
  - name: profile
    persistentVolumeClaim: {claimName: bw-runtime-pvc}
  - name: tmp
    emptyDir: {sizeLimit: 256Mi}
  - name: shm
    emptyDir: {medium: Memory, sizeLimit: 256Mi}
  - name: home
    emptyDir: {sizeLimit: 256Mi}
---
apiVersion: v1
kind: Service
metadata:
  name: bw-runtime-worker
  namespace: burrowser
  labels:
    burrowser/test: runtime-smoke
spec:
  type: ClusterIP
  selector:
    burrowser/test: runtime-smoke
  ports:
  - {name: rpc, port: 8080, targetPort: rpc}
