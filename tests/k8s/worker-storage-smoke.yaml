apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ab-smoke-pvc
  labels:
    app.kubernetes.io/name: agent-browser-worker
    agent-browser/test: storage-smoke
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: agent-browser-local-path
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: Pod
metadata:
  name: ab-smoke-worker
  labels:
    app.kubernetes.io/name: agent-browser-worker
    agent-browser/test: storage-smoke
spec:
  automountServiceAccountToken: false
  serviceAccountName: agent-browser-worker
  containers:
  - name: worker
    image: agent-browser-worker:dev
    imagePullPolicy: Never
    command: [node, -e]
    args: ["require('fs').mkdirSync('/profile/chromium',{recursive:true}); require('fs').writeFileSync('/profile/chromium/smoke.txt','ok'); setTimeout(()=>{}, 3600000)"]
    securityContext:
      runAsNonRoot: true
      allowPrivilegeEscalation: false
      readOnlyRootFilesystem: true
      capabilities:
        drop: [ALL]
    volumeMounts:
    - name: profile
      mountPath: /profile
  volumes:
  - name: profile
    persistentVolumeClaim:
      claimName: ab-smoke-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: ab-smoke-worker
  labels:
    agent-browser/test: storage-smoke
spec:
  type: ClusterIP
  selector:
    agent-browser/test: storage-smoke
  ports:
  - name: rpc
    port: 8080
    targetPort: 8080
